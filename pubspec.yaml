name: kiss_typography
description: "A new Flutter package project."
version: 0.5.0
homepage: https://github.com/WAMF/kiss_typography

environment:
  sdk: ^3.6.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
